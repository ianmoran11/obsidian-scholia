Scholia metadata task completed on 2026-05-03.

Implemented:
- Added typed LLM stream events so streaming can yield content plus final metadata.
- Updated OpenRouter streaming requests to ask for usage data and tolerate missing usage or cost fields.
- Preserved usage and cost payloads from SSE events instead of discarding non-content data.
- Added run metadata modeling and formatting for model, prompt tokens, completion tokens, total tokens, cost, duration, and provider.
- Added visible callout metadata lines after successful inline generation when showRunMetadata is enabled.
- Added metadata recording to markdown and JSON-line capture output.
- Added a showRunMetadata setting, defaulting to true.

Verification:
- npm test passed: 11 test files, 153 tests.
- npm run build passed.
- Computer Use was used to inspect the running Obsidian desktop window and confirm the Scholia templates are visible in the active vault.
- Follow-up fix: adjusted callout metadata formatting so the stream writer inserts it as a normal blockquoted metadata line instead of nested blockquote text.

Scholia repeat questions in callout task completed on 2026-05-03.

Implemented:
- Added optional questionText support to Scholia callout skeletons.
- Custom-probe inline generations now pass the user's query into the callout skeleton.
- Visible custom-probe callouts now render Question before Response while preserving Context.
- Markdown and JSON-line central captures now store the custom-probe question.

Verification:
- npm test -- --run passed: 11 test files, 159 tests.
- npm run build passed.
- Browser Use was used to open a local evidence page and confirm the visible callout contains the Question section.
- Screenshot saved at evidence/screenshots/question-callout-demo.png.

Scholia regenerate option task completed on 2026-05-03.

Implemented:
- Added a durable scholia:run snapshot comment to new inline callouts with template, source, question, context scope, model, token, reasoning, and callout options.
- Added callout parsing that finds generated Scholia callouts, reads snapshots, and identifies response/metadata replacement boundaries.
- Added Scholia: Regenerate current callout command.
- Added a rendered Regenerate button for callouts with a valid snapshot, using the same regeneration service as the command.
- Regeneration re-extracts fresh context, replays the original template/question/run options, temporarily marks the callout pending, and replaces only the response plus metadata.

Verification:
- npm test -- --run passed: 12 test files, 162 tests.
- npm run build passed.
- Computer Use was used to inspect the rendered regenerate evidence page and confirm the visible Regenerate button.
- Screenshot saved at evidence/screenshots/regenerate-callout-demo.png.

Scholia allow for chat task completed on 2026-05-03.

Implemented:
- Added chatFollowupsEnabled setting, defaulting to true, with a settings-tab toggle.
- Custom-probe inline runs now detect when the cursor is inside a generated Scholia callout and append a follow-up turn instead of creating a new callout.
- Follow-up requests include current note context, stripped prior callout conversation, and the new follow-up question.
- Follow-up responses stream at the end of the existing callout and write per-turn metadata when showRunMetadata is enabled.
- Added helpers to format follow-up sections and strip run comments/metadata from chat history.

Verification:
- npm test -- --run passed: 12 test files, 165 tests.
- npm run build passed.
- Browser Use / Computer Use was used to inspect the rendered chat follow-up evidence page and confirm the follow-up appears inside the same Scholia callout.
- Screenshot saved at evidence/screenshots/chat-followup-demo.png.

Scholia add audio option task completed on 2026-05-03.

Implemented:
- Added DeepInfra audio settings for API key, enablement, TTS model, optional voice, and output folder.
- Added an independent DeepInfra TTS client using the current native endpoint: POST https://api.deepinfra.com/v1/inference/{model_name}.
- Updated the TTS client after live verification to decode DeepInfra JSON data-URI audio responses as well as binary audio responses.
- Updated the TTS request body to ask DeepInfra for mp3 output by default.
- Added note/callout text extraction that strips frontmatter, comments, metadata, and audio embeds before TTS.
- Added vault binary audio storage under the configured folder with stable note/callout timestamped filenames.
- Added Scholia: Generate audio for current note/callout command.
- Callout audio inserts or updates a blockquoted Audio line; whole-note audio inserts or updates a note-level Scholia Audio section.
- Kept the DeepInfra API key confined to settings and request headers; it is not written to notes, metadata, tests, logs, or progress.

Verification:
- npm test -- --run passed: 14 test files, 171 tests.
- npm run build passed.
- npx tsc --noEmit passed.
- Computer Use was used to inspect the rendered audio verification page and confirm the command, settings behavior, callout embed, and note-level embed behavior are visible.
- Screenshot saved at evidence/screenshots/audio-option-demo.png.
- A live DeepInfra request with neutral verification text produced a WAV file at evidence/screenshots/scholia-audio-verification.wav.
- A second live DeepInfra request with 500 neutral words produced a 10 MB WAV file at evidence/screenshots/scholia-audio-500-token-verification.wav.
- A follow-up live DeepInfra request with 500 neutral words and output_format=mp3 produced evidence/screenshots/scholia-audio-500-token-verification.mp3.

Scholia Spaced Repetition integration task completed on 2026-05-03.

Implemented:
- Confirmed the current Obsidian Spaced Repetition card syntax from the plugin docs: deck tags such as #flashcards, basic cards with Question::Answer, multiline cards with ?, and cloze cards with ==deleted text==.
- Added template frontmatter options for spaced_repetition, sr_format, sr_deck, and sr_tags.
- Added a global Spaced Repetition integration setting, defaulting to false.
- Added deterministic SR formatting helpers for basic, multiline, and cloze cards.
- Updated captures and append output so enabled flashcard templates write parseable SR markdown instead of blockquoted callout text.
- Updated inline-only SR runs to append a separate parseable card after the readable Scholia callout.
- Updated the sample Flashcard template to opt into SR basic format and #flashcards/scholia.

Verification:
- npm test -- --run passed: 15 test files, 180 tests.
- npx tsc --noEmit passed.
- npm run build passed.
- Computer Use was used to inspect the rendered evidence page showing readable Scholia output beside SR-compatible markdown.
- Screenshot saved at evidence/screenshots/spaced-repetition-demo.png.
