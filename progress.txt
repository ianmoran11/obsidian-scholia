Scholia metadata task completed on 2026-05-03.

Implemented:
- Added typed LLM stream events so streaming can yield content plus final metadata.
- Updated OpenRouter streaming requests to ask for usage data and tolerate missing usage or cost fields.
- Preserved usage and cost payloads from SSE events instead of discarding non-content data.
- Added run metadata modeling and formatting for model, prompt tokens, completion tokens, total tokens, cost, duration, and provider.
- Added visible callout metadata lines after successful inline generation when showRunMetadata is enabled.
- Added metadata recording to markdown and JSON-line capture output.
- Added a showRunMetadata setting, defaulting to true.

Verification:
- npm test passed: 11 test files, 153 tests.
- npm run build passed.
- Computer Use was used to inspect the running Obsidian desktop window and confirm the Scholia templates are visible in the active vault.
- Follow-up fix: adjusted callout metadata formatting so the stream writer inserts it as a normal blockquoted metadata line instead of nested blockquote text.

Scholia repeat questions in callout task completed on 2026-05-03.

Implemented:
- Added optional questionText support to Scholia callout skeletons.
- Custom-probe inline generations now pass the user's query into the callout skeleton.
- Visible custom-probe callouts now render Question before Response while preserving Context.
- Markdown and JSON-line central captures now store the custom-probe question.

Verification:
- npm test -- --run passed: 11 test files, 159 tests.
- npm run build passed.
- Browser Use was used to open a local evidence page and confirm the visible callout contains the Question section.
- Screenshot saved at evidence/screenshots/question-callout-demo.png.
